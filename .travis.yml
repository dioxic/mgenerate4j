#sudo: false
language: java
#install: skip

os: linux
dist: xenial

jdk:
  - openjdk8

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/

#cache:
#  directories:
#    - $HOME/.gradle/caches/
#    - $HOME/.gradle/wrapper/

#addons:
#  apt:
#    packages:
#      - oracle-java10-installer




#script:
#  - chmod 700 ./mvnw
#  - ./mvnw clean install
#  - cd test
#  - ../mvnw clean test

#env:
#  global:
#  - SONATYPE_USERNAME=dioxic

#after_success:
#  - cd ..
#  - python addsettings.py
#  - cd bsoncodec-apt
#  - mvn clean deploy -DskipTests=true --settings ~/.m2/mySettings.xml

#cache:
#  directories:
#  - $HOME/.m2
